Use case: precise-object-edit.
Asset type: one complete side-view FIREWALL enemy sprite for a 2D industrial action game.
Edit target/reference: use ONLY the bottom-right furnace machine in the supplied 2x2 sheet. Make a standalone revised sprite of that character. Preserve its battered gunmetal, rusty orange armor, red-orange recessed glowing horizontal heat bars, round mechanical fittings, visible material volume and top-left lighting.
Change: redesign its overall silhouette to be a VERY SLIM TALL RECTANGLE, overall body width at most 42 percent of full body height (height 2.4 times width). The existing collision footprint is 60 pixels wide by120 pixels tall; the full furnace should fill that tall envelope when uniformly downscaled. Two narrow side pillars flank one tall central furnace grate, with THREE thick horizontal glowing warm red-orange bars separated by substantial dark gaps. Two short slender exhaust pipes on top. A compact narrow continuous tread base NO WIDER than the vertical pillar pair. No outstretched arms, no side pods, no humanoid form, no broad tank chassis. Keep every round bearing truly circular; redesign the character instead of squashing the original image.
Composition: one isolated complete object centered, very tall and narrow, full head-to-tread body visible with margin on every side. Slight 2.5D side depth and facing LEFT, matching the source's camera. Strong clear silhouette and large functional shapes that read at 60x120 pixels.
Background: ACTUAL transparent RGBA alpha, including gaps between exhaust pipes; never draw a transparency checkerboard, white/gray backdrop, ground, shadow, labels, borders, text or watermark.
Only one furnace machine, no other enemy or atlas quadrants.
