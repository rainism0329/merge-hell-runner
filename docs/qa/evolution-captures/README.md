# Actual development captures

Captured through the production simulation and frame publication path in a headless LAB
preview on 2026-09-10, Oracle JDK 17 / IntelliJ 232 API. These are actual game renders, not
concept illustrations. Menu, combat and Legacy Boss are 960 × 600. Settings is 600 × 400
with the real compact/high-contrast mode. No collage, replacement UI or post-processing
was applied. The HP-boundary fix subsequently passed formal tests and does not affect
the captured scenes.

The illustrated city is loaded. Hero/enemy rigs remain vector fallbacks while valid transparent
exports are pending. These captures are not evidence of ordinary progression, completed
campaign playthrough, audio audition, or installation inside a live IDE.

Full scenarios, responsive variants and 96 consecutive motion frames can be regenerated by
`src/test/java/com/bigphil/mergehell/GameVisualPreview.java`; local evidence is in
`build/visual-preview/` and `build/compact-validation/`. See the parent integrated-upgrade
record for scope and limitations.
